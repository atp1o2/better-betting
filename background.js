// func: matchProps()
// func: compareBets()
// func: sendBets()

function matchPlayersProps(grabBovada(), grabDimes()) {
  // INPUT: two arrays containing objects
  // OUTPUT:
  // NESTED LOOP: through Bovada array of objects(bo)
    // LOOP: Dimes array of objects(di)
      // use names from both objects
      // IF name === name && type.size === type.size
        // run compareBets(bo.plus, bo.neg, di.plus, di.neg)
        // IF valid bets
          //
}


function compareBets(bo.plus, bo.neg, di.plus, di.neg) {
  // IF bo.plus > di.neg
    // success
  // IF di.plus > bo.neg
    // success
}

function sendBets(){
  // logic for sending bet info to popup.html
}